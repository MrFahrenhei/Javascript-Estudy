function pow(x, y) {
    return Math.pow(x, y);
}
console.log(pow(2, 2));
function pow2(x, y) {
    return Math.pow(x, y).toString();
}
console.log(pow2(2, 2));
function pow3(x, y) {
    Math.pow(x, y);
}
console.log(pow3(2, 2));
